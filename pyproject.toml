[project]
name = "gpuledger"
version = "0.1.0"
description = "AI training cost calculator"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35.0",
    "pandas>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]